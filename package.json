{
  "scripts": {
    "build": "rm  ./dist/* && cp appsscript.json ./dist && webpack",
    "push": "npm run build && clasp push"
  },
  "dependencies": {
    "@types/google-apps-script": "^1.0.78"
  },
  "devDependencies": {
    "gas-webpack-plugin": "^2.5.0",
    "ts-loader": "^9.5.1",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
